# Backend API base URL (used by lib/api.ts)
# Default: /api/v1 (proxied via Next.js rewrites to backend)
NEXT_PUBLIC_API_BASE_URL=/api/v1

# WebSocket URL (used by lib/websocket.ts)
# Default: derived from window.location if not set
NEXT_PUBLIC_WS_BASE_URL=ws://127.0.0.1:8000/api/v1
